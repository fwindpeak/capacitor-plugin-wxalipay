import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fwindpeak.wxalipayexample',
  appName: 'wxalipay-plugin-example',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
